<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Система контролю часу" Height="450" Width="600">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,10">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBox Grid.Column="0" Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" VerticalAlignment="Center" Padding="5"/>
      <Button Grid.Column="1" Content="Додати" Command="{Binding AddTaskCommand}" Padding="10,5"/>
    </Grid>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*"/>
        <ColumnDefinition Width="1.5*"/>
      </Grid.ColumnDefinitions>

      <ListBox Grid.Column="0" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" HorizontalContentAlignment="Stretch">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid Margin="0,5">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>

              <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,10,0"/>
              <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center"/>
              <Button Grid.Column="2" Content="Видалити" Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}" Margin="10,0,0,0" Padding="5,2"/>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <Border Grid.Column="1" Margin="10,0,0,0" Padding="10" Background="#FFF5F5F5" CornerRadius="5">
        <StackPanel VerticalAlignment="Center">
          <TextBlock Text="Активна задача:" Foreground="Gray" FontSize="12" HorizontalAlignment="Center"/>
          <TextBlock Text="{Binding SelectedTask.Title}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,15" TextWrapping="Wrap" TextAlignment="Center"/>

          <TextBlock Text="{Binding SelectedTask.FormattedTime, TargetNullValue='00:00:00', FallbackValue='00:00:00'}"
                     FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Старт" Command="{Binding StartTimerCommand}" Margin="0,0,5,0" Padding="10"/>
            <Button Grid.Column="1" Content="Стоп" Command="{Binding StopTimerCommand}" Margin="5,0,0,0" Padding="10"/>
          </Grid>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Window>